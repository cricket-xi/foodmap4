version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # depends_on:
    #   - backend

  # Example backend service (if applicable)
  # backend:
  #   image: openjdk:17-jdk-alpine
  #   volumes:
  #     - ./backend/target/campusfoodie-0.0.1-SNAPSHOT.jar:/app/app.jar
  #   command: java -jar /app/app.jar
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=prod
  #     - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/campusfoodie
  #   depends_on:
  #     - db

  # db:
  #   image: mysql:8.0
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=root
  #     - MYSQL_DATABASE=campusfoodie
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
