<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusfoodie.mapper.PlatformLogMapper">

    <select id="findAll" resultType="com.campusfoodie.model.PlatformLog">
        SELECT * FROM platform_log ORDER BY created_at DESC
    </select>

    <select id="findByEntityType" resultType="com.campusfoodie.model.PlatformLog">
        SELECT * FROM platform_log WHERE actor_role = #{actorRole} ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO platform_log (action_type, actor_id, actor_role, target_id, description, created_at)
        VALUES (#{actionType}, #{actorId}, #{actorRole}, #{targetId}, #{description}, NOW())
    </insert>

</mapper>
